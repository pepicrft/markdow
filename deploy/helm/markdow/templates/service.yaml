apiVersion: v1
kind: Service
metadata:
  name: {{ include "markdow.fullname" . }}
  labels:
    {{- include "markdow.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "markdow.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
